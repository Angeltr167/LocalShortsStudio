name: Publish Docker image

on:
  push:
    branches:
      - main

concurrency:
  group: docker-ghcr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cancel-previous:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary no-op run to cancel the inherited publish workflow."
